import Foundation

/// Checks for any edge cases and returns recursive call to MergeSortHelper
/// - Parameter arr: array to be sorted
/// - Returns: sorted array
public func MergeSort<T: Comparable>(_ arr: [T]) -> [T] {
    return arr
}

/// Recursively sorts arrays by dividing and conquering
/// - Parameter arr: array to be sorted
/// - Returns: sorted array
private func MergeSortHelper<T: Comparable>(_ arr: [T]) -> [T] {
    return arr
}

/// Merges two arrays from smallest to largest
/// - Parameters:
///   - left: the first array to merge from
///   - right: the second array to merge from
/// - Returns: The combined left and right arrays
private func Merge<T: Comparable>(_ left: [T], _ right: [T]) -> [T] {
    return left
}
